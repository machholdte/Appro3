

const express = require('express');
const app = express();
const port = 3000;




// Endpoint med login

// Serveren lytter til port.
server.listen(port, () => {
    console.log(`Serveren lytter på port ${port}`)
  })

